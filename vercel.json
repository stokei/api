{
  "version": 2,
  "buildCommand": "yarn db:generate && nest build",
  "installCommand": "yarn install",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "dist/main.js" }]
}
